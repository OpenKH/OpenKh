<Window x:Class="OpenKh.Tools.Kh2FontImageEditor.Views.SpacingWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:OpenKh.Tools.Kh2FontImageEditor.Views"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen"
        Title="SpacingWindow" Height="450" Width="800">
    <DockPanel LastChildFill="True">
        <StackPanel Orientation="Horizontal" DockPanel.Dock="Bottom" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="7">
            <Button Padding="7" Command="{Binding State.SaveCommand}">
                <AccessText>_Save and close</AccessText>
            </Button>
        </StackPanel>
        <Label DockPanel.Dock="Top" HorizontalAlignment="Left" VerticalAlignment="Top">Spacing mapping. Left click the glyph to narrow. Right click the glyph to widen.</Label>
        <Viewbox>
            <Image Source="{Binding State.Image}" Width="{Binding State.Width}" Height="{Binding State.Height}"
                   IsHitTestVisible="True"
                   Name="_image"
                   />
        </Viewbox>
    </DockPanel>
</Window>
