<UserControl x:Class="OpenKh.Tools.ModsManager.Views.CollectionOptionalModDetailsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:OpenKh.Tools.ModsManager.Views"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="256">
    <UserControl.Resources>
        <Style TargetType="Expander">
            <Setter Property="Foreground" Value="{Binding ColorTheme.TextColor}"/>
        </Style>
    </UserControl.Resources>
    <StackPanel>
        <TextBlock Margin="3 0 0 3" FontWeight="Bold" Text="{Binding Name}" d:Text="Mod name"/>
        <TextBlock Margin="3 0 0 3" Text="{Binding Author}" d:Text="Author name"/>
    </StackPanel>
</UserControl>
